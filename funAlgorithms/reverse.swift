import UIKit

var sample = "Hola a todos mis amigos que están aprendiendo a hacer swift y que tienen una macbook pro con batería muy pobre"

let words = sample.components(separatedBy: " ")

var w = "carlo"

let wr = String(w.reversed())

for c in w.characters { print(c) }

/*
If I don't import UIKit I couldn't use protocols as componets and reversed

*/
